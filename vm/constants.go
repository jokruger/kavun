package vm

const (
	// GlobalsSize is the default number of global variables for a VM.
	GlobalsSize = 1024

	// DefaultStackSize is the default stack size for a VM.
	DefaultStackSize = 2048

	// DefaultMaxFrames is the default number of function frames for a VM.
	DefaultMaxFrames = 1024
)
